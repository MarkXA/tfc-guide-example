apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${FRONTNAME}
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${FRONTNAME}
  template:
    metadata:
      labels:
        app: ${FRONTNAME}
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: ${FRONTNAME}
        image: mcr.microsoft.com/azuredocs/${FRONTNAME}:v1
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
        ports:
        - containerPort: 80
        env:
        - name: REDIS
          value: "azure-vote-back"
