apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${var.frontName}
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${var.frontName}
  template:
    metadata:
      labels:
        app: ${var.frontName}
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: ${var.frontName}
        image: mcr.microsoft.com/azuredocs/${var.frontName}:v1
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
        ports:
        - containerPort: 80
        env:
        - name: REDIS
          value: "azure-vote-back"
